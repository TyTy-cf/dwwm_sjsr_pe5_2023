{% extends 'back/base.html.twig' %}

{% block title %}
    {{ 'back.pages.publisher.new.title'|trans }}
{% endblock %}

{% block body %}

    <div class="d-flex my-4">
        <a class="main-link my-auto me-3" href="{{ path('app_admin_publisher_index') }}">
            <i class="fa fa-arrow-left fa-2x"></i>
        </a>
        <h1 class="my-4">{{ 'back.pages.publisher.new.title'|trans }}</h1>
    </div>

    {{ include('back/pages/publisher/_form.html.twig') }}

{% endblock %}
